export declare class LayeredDigraphNetwork {
    /** The name of this network. */
    name: string;
    /** The vertices in this network. */
    vertexArray: any[];
    /** The edges in this network. */
    edgeArray: any[];
    /** The comments in this network. */
    commentArray: any[];
    constructor(name: string);
}
